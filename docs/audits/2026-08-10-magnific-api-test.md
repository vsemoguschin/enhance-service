# Magnific API — проверка ключа и пригодности как внешнего провайдера

**Дата:** 2026-08-10 · **Ветка:** `agent/2026-08-10-magnific-api-test` · **Статус:** аудит + инструмент теста

## Задача

Появился ключ Magnific API (+ webhook signing secret). Проверить: годится ли внешний API как
движок апскейла вместо простаивающего локального Real-ESRGAN, сколько стоит и какого качества
результат. Тест — до любой интеграции в book-editor.

## Аудит

### Текущее состояние фичи

Улучшение фото реализовано целиком (C1 `enhance-service` / C2 `file-platform` / C3 `book-editor`),
задеплоено на codex и **выключено** флагом `ENHANCE_ENABLED=false`: на codex нет GPU, Real-ESRGAN x4
не укладывается и в 4.5 минуты на фото (llvmpipe, 2 ядра) — см. [../operations.md](../operations.md).
Развилка зафиксирована там же и в `#/#tasks/file-platform.md`: **GPU-бокс** (стратегия, ~1–3 с/фото,
сервис переносится как есть) либо **внешний API** («запустить прямо сейчас»).

### Что сейчас делают в веб-версии Magnific (со слов владельца)

Промпт «подготовь фото к профессиональной печати… разрешение 4к…» + модель **Seedream 5 pro**,
расход ~1500 кредитов, на выходе JPEG в килобайты.

Это **не апскейлер**: Seedream — генеративная модель (`/api-reference/image-editing/seedream-4-5-edit/`,
`/api-reference/text-to-image/post-seedream-v4-5`). Следствия для печати:

- **сходство не гарантировано** — модель перерисовывает изображение, а не увеличивает его;
- **выход — сжатый JPEG фиксированного размера**, «4к» из промпта не контракт, а пожелание;
  килобайтный файл на 300 DPI непечатаем;
- **цена в 1500 кредитов** против €0.10–0.20 у профильного апскейлера.

Правильный инструмент под нашу задачу — `image-upscaler-precision` (сохраняет содержимое, текстуру и
структуру, детали не выдумывает). `image-upscaler-creative` — только для сравнения: он умеет дорисовывать,
для лиц реальных клиентов это риск. Цветокоррекция («фото тёмное») — отдельный шаг, дешевле делать
локально (OpenCV/PIL уже в зависимостях), а не платным генеративным проходом.

### Контракт Magnific (сверено с docs.magnific.com)

| | Creative | Precision V2 |
|---|---|---|
| POST | `/v1/ai/image-upscaler` | `/v1/ai/image-upscaler-precision-v2` |
| вход | base64 | base64 **или** публичный HTTPS-URL |
| масштаб | `2x/4x/8x/16x` | `2..16` |
| ручки | `optimized_for`, `prompt`, `creativity`/`hdr`/`resemblance`/`fractality` (−10..10), `engine` | `flavor` (`sublime/photo/photo_denoiser`), `sharpen`, `smart_grain`, `ultra_detail` |

Общее: заголовок `x-magnific-api-key`; ответ `{data:{task_id,status,generated[]}}`;
статусы `CREATED|IN_PROGRESS|COMPLETED|FAILED`; результат — poll `GET {endpoint}/{task_id}` либо `webhook_url`.
Лимиты: выход ≤ 25.3 Мпикс, 50 запросов/мин на ключ. Ключи Freepik работают на обоих хостах.

**Вебхуки:** заголовки `webhook-id`, `webhook-timestamp`, `webhook-signature`;
подпись = base64(HMAC-SHA256(secret, `webhook-id.webhook-timestamp.body`)), формат значения `v1,<sig>`.
Для локального теста не нужны (нет публичного URL) — используем polling.

**Цена (docs, Precision):** 640×480→2x ≈ €0.10, 640×480→4x ≈ €0.20, 1920×1080→2x ≈ €0.20 —
считается по площади выхода. В кредитах не опубликована → меряем фактическое списание в тесте.

### Риски

1. **Приватность** — фото клиентов уходят наружу. В [../задача.md](../задача.md) внешний API признан
   нежелательным для прода («максимум для быстрой проверки»); для теста берём не клиентский портрет.
2. **Размер выхода** — file-platform принимает ≤20 МБ, наш кап `ENHANCE_MAX_OUTPUT_PX=7200`.
   Magnific на 8x/16x это перебьёт → downscale + JPEG обязательны на нашей стороне.
3. **Таймаут** — book-editor ждёт результат максимум 180с (`ENHANCE_MAX_WAIT_MS`), HTTP-таймаут 30с
   на вызов. Если Magnific стабильно дольше — C3 придётся переводить на webhook/async.
4. **Egress с прода** — на codex OUTPUT для uid 109 закрыт по портам, весь исходящий в OpenVPN.
   Доступность `api.magnific.com` с бокса проверяется отдельно; локальный успех ничего не доказывает.

## План

- **L0 (эта ветка):** `scripts/magnific_smoke.py` — один вызов, замер времени/веса/разрешения и
  фактического списания кредитов. Сравнить precision vs creative на одном фото, свериться с тем,
  что даёт Seedream в вебе.
- **L1:** если качество/цена устраивают — второй провайдер в `enhance-service`
  (`ENHANCE_PROVIDER=local|magnific`), контракт C1 не меняется, book-editor и file-platform не трогаем.
- **L2:** включение `ENHANCE_ENABLED=true` + проверка egress с бокса.

## Результат

### Прогоны (2026-08-10)

| Движок | Вход | Выход | Время | Итог |
|---|---|---|---|---|
| precision-v2 | 1000×750 (уменьшенная копия) | 2000×1500, 4.95 МБ | 81с | ОК, но запрошен x4 — вернулся **x2** |
| precision (v1) | то же | — | 67с | `COMPLETED` с пустым `generated[]`, кредиты впустую |
| local_fix (PIL) | 4000×3000 оригинал | 3000×4000, 3.15 МБ | <1с | 0 кредитов |

Ключ рабочий, e2e через API проходит. Суммарный расход по кабинету — **180 кредитов**.
Кредиты в заголовках ответа не отдаются, только `X-RateLimit-*` (50/мин).

### Главный вывод: апскейлер решает не ту задачу

Реальное фото клиента (`reference-img1.jpeg`) — **пересъёмка бумажного отпечатка**: 12 Мп «пустых»
пикселей, EXIF-поворот, блик от вспышки, выцветший цвет, зерно фотобумаги, отсутствие деталей.
Апскейл здесь бесполезен: 12 Мп = 34×25 см при 300 DPI (печатать уже можно), а Precision по
определению не выдумывает детали — он увеличит зерно и блик. Плюс scale=2 от 12 Мп = 48 Мп,
что выше лимита 25.3 Мп: полноразмерный оригинал API вообще не примет.

Веб-результат владельца (Seedream 5 pro, ~1500 кредитов) действительно лучше: блик убран, цвет
восстановлен, детали резкие. Но это **перерисовка**, и у неё своя цена:
- выход **1328×1760 = 2.3 Мп**, то есть 11×15 см при 300 DPI — для разворота фотокниги мало,
  поверх всё равно нужен апскейл;
- кадр сменил пропорции 4:3 → 3:4 (края срезаны);
- лицо ребёнка реконструировано моделью — для детских фото в фотокниге сходство проверить нечем.

### Что бесплатно

`scripts/local_fix.py` (EXIF-ориентация + автоуровни + gray-world баланс белого + unsharp) за доли
секунды и 0 кредитов возвращает ориентацию, цвет и контраст — заметную часть разницы с вебом.
Не лечит: блик вспышки, зерно бумаги, отсутствующую детализацию.

**Следствие для пайплайна:** локальную коррекцию делать всегда и до платного вызова — иначе платим
за то, что чинится даром. Дальше по классу фото: «мало пикселей, кадр нормальный» → precision
(180 кредитов); «убитый скан» → генеративное восстановление (Seedream edit есть и в API) с
осознанным риском сходства, при необходимости + precision поверх.

### Seedream через API — цель достигнута

Владелец: ориентир — веб-результат, «нам главное чтобы изображение было хорошим визуально».
Значит нужен не апскейлер, а тот же генеративный проход, но через API.

`POST /v1/ai/text-to-image/seedream-v4-5-edit` — `prompt` (≤4096), `reference_images` (1–5, URL или
base64, ≤10 МБ, ≥256×256), `aspect_ratio` (`traditional_3_4` и др.), `seed`, `webhook_url`.
Выход до 4 Мп. В API доступен Seedream **4.5**, в вебе владелец пользуется **5 pro**.

Прогон на локально исправленном фото, промптом из веб-версии:

| | вход | выход | время |
|---|---|---|---|
| веб (5 pro) | оригинал | 1328×1760 = **2.3 Мп**, 0.44 МБ | — |
| API (4.5 edit) | `_localfix.jpg` | 1774×2364 = **4.2 Мп**, 1.05 МБ | **28с** |

Визуально сопоставимо с вебом: блик вспышки убран, цвет живой, зерно бумаги ушло, кадр и поза
сохранены. При этом **вдвое больше пикселей** (15×20 см против 11×15 при 300 DPI) и втрое быстрее
апскейлера. Лицо, как и в вебе, реконструировано — это свойство метода, не дефект прогона.

### Рабочий пайплайн

1. **`local_fix`** — EXIF-ориентация, автоуровни, баланс белого. 0 кредитов, <1с. Всегда первым:
   Seedream не должен тратиться на то, что чинится даром, и не должен угадывать ориентацию.
2. **`seedream-edit`** — восстановление и «красота». 28с.
3. **`precision-v2 x2`** — опционально, если макет крупнее 15×20 см: 1774×2364 → 3548×4728
   (16.8 Мп, в лимит 25.3 укладывается) = 30×40 см при 300 DPI.

### Экономика (замерено по кабинету)

| Вызов | Кредитов | Время |
|---|---|---|
| `local_fix` (локально) | **0** | <1с |
| `seedream-edit` (4.5, API) | **50** | 24–28с |
| `precision-v2` | ~90 | 81–131с |
| тот же результат через **веб** (5 pro) | **1500** | — |

Генеративное восстановление через API в **30 раз дешевле веб-версии** и вдвое дешевле апскейлера.
Полный пайплайн одного фото до печатного размера — ~140 кредитов и ~2.5 минуты.

### Прогон 2: низкорес-селфи (типовой кейс фотокниги)

`image.png` 758×647 (0.5 Мп = 6×5 см при 300 DPI), два лица в очках — сходство критично.

1. `local_fix --no-wb --sharpen 1.0` — только уровни; баланс белого выключен, иначе убьёт закатный свет.
2. `seedream-edit --preset restore` → 2364×1774 (4.2 Мп) за 24с. Лица, очки, узор рубашки, волосы —
   сохранены; шум и мыло ушли.
3. `precision-v2 --scale 2` → 4728×3548 (16.8 Мп = **40×30 см** при 300 DPI) за 131с. Артефактов не внёс.

Побочные эффекты, важные для интеграции:
- **Кадр расширился**: 758×647 это 1.17:1, а `aspect_ratio` у Seedream — только пресеты
  (`classic_4_3` ближайший). Модель дорисовала края (трава слева, дом справа). Исходную пропорцию
  при необходимости возвращать кропом локально — бесплатно.
- **27 МБ на выходе** — выше лимита file-platform (20 МБ): при интеграции обязательно пережатие.
- **131с** у апскейлера против 180с лимита в book-editor — на крупных входах не уложится.
- `--scale 2` отработал ровно как x2, то есть параметр рабочий; расхождение первого прогона
  (просил 4 — получил 2) осталось невоспроизведённым.

### Растяжение кадра и как оно лечится

Причина: у Seedream пропорция выхода выбирается из фиксированного списка. Кадр 758×647 (1.171)
был отправлен с `classic_4_3` (1.333) — модель **растянула** изображение по ширине, лица стали шире.
Это не outpainting, а деформация.

Лечение — `scripts/fit_aspect.py`, оба шага локальные и бесплатные:

1. `--mode pad --to 4:3` — дополняем зеркальными полями: 758×647 → 863×647 (1.334);
2. Seedream с `classic_4_3` — растягивать больше нечего, кадр отдаётся 1:1 по геометрии;
3. `--mode crop --to 758:647` — срезаем поля: 2364×1774 → **2078×1774 (1.171)**.

Проверено на том же фото: пропорции лиц совпадают с оригиналом, детализация сохранена.

### Nano Banana (Gemini 2.5 Flash Image)

`POST /v1/ai/gemini-2-5-flash-image-preview` — `prompt` + `reference_images`, **без параметра
пропорций**: кадр берётся от входа, поэтому растяжения не бывает в принципе.

| | Seedream 4.5 (+pad/crop) | Nano Banana |
|---|---|---|
| выход | 2078×1774 — **3.7 Мп** | 1120×928 — **1.0 Мп** |
| печать при 300 DPI | 18×15 см | 9×8 см |
| время | 21с | **14с** |
| пропорция | верная (после pad/crop) | верная сама по себе |
| детализация | выше, текстуры проработаны | мягче, ближе к исходному мылу |

Вывод: Nano Banana быстрее и не требует возни с пропорциями, но 1 Мп для печати мало.
Под фотокнигу основной путь — Seedream + pad/crop; Nano Banana годится для превью и веба.

### Экономика (уточнено по кабинету)

| Вызов | Кредитов |
|---|---|
| `local_fix`, `fit_aspect` | **0** |
| `seedream-edit` | **50** |
| `nano-banana` | уточняется |
| `precision-v2`, выход 3 Мп | ~90 |
| `precision-v2`, выход 16.8 Мп | **270** |
| веб-версия (Seedream 5 pro) | 1500 |

Цена апскейла растёт с площадью результата — это самая дорогая часть пайплайна.
Восстановление (50) дешевле апскейла в 2–5 раз, поэтому апскейл включать только когда
макет реально крупнее того, что отдаёт генеративный шаг.

### Детализация против сходства

Эталон владельца (`image copy.png`, 1120×956 = 1.1 Мп) детальнее наших генеративных прогонов
на пиксель, но модель там **изменила мимику**: в оригинале губы сомкнуты, в эталоне — открытая
улыбка с зубами. Это общий закон: чем больше деталей модель выдумывает, тем меньше сходство.

Проверено на Creative-апскейлере, две настройки на одном фото:

| Настройки | Выход | Лица |
|---|---|---|
| `creativity 0, hdr 6, resemblance 8, fractality 2`, вход = оригинал | 3024×2576 (7.8 Мп) | **сломаны**: тёмные линзы очков стали прозрачными, сквозь них видны глаза; лица помолодели |
| `creativity -10, hdr 4, resemblance 10, engine magnific_sharpy`, вход = результат Seedream | 4152×3544 (**14.7 Мп**) | **сохранены**: очки тёмные, черты и улыбка как в оригинале |

Ручки, которые за это отвечают: `creativity` — выдумывание (держать на −10), `resemblance` —
удержание оригинала (+10), `hdr` — микроконтраст и детали (безопасно поднимать), `engine`
`magnific_sharpy` — резкость без галлюцинаций. Дальнейший запас по детализации — `hdr` до 8–10
при неизменных `creativity −10` / `resemblance +10`.

### Финальный пайплайн (проверен end-to-end)

| Шаг | Инструмент | Кредитов |
|---|---|---|
| 1. ориентация, уровни, баланс белого | `local_fix.py` | 0 |
| 2. подгонка пропорции | `fit_aspect.py --mode pad` | 0 |
| 3. восстановление | `seedream-edit --preset restore` | 50 |
| 4. возврат пропорции | `fit_aspect.py --mode crop` | 0 |
| 5. детализация + печатный размер | `creative` (sharpy, creativity −10, resemblance 10) | по площади |

Результат на реальном кейсе: 758×647 (0.5 Мп, 6×5 см) → **4152×3544 (14.7 Мп, 35×30 см)**,
лица узнаваемы, ~1.5 минуты машинного времени. Шаг 5 включать только под крупные макеты —
он самый дорогой.

### Развилка: сходство лиц или детализация

Владелец забраковал финал по лицу мужчины. Причина системная: **любой генеративный шаг
переписывает лицо**. Деталей, которых нет в исходных 0.5 Мп, взять неоткуда — их можно только
выдумать, а выдуманное лицо перестаёт быть лицом конкретного человека.

Отсюда два непересекающихся маршрута:

| | Только апскейл (без генерации) | С генеративным восстановлением |
|---|---|---|
| путь | `local_fix` → `creative` sharpy (creativity −10, resemblance +10) | `local_fix` → pad → `seedream` → crop → апскейл |
| выход | 3024×2576 (7.8 Мп), 27с | 4156×3548 (14.7 Мп) |
| лица | **сохранены** — очки тёмные, черты исходные | **переписаны** — другой человек |
| картинка | мягче, видно происхождение из 0.5 Мп | «красиво», текстуры богатые |

Для фотокниги, где заказчик знает людей на снимке, приоритет — левая колонка.

### Ещё не испробованные варианты

1. **Только `precision`** — нулевое выдумывание, максимум верности, но мыло увеличивается как есть.
2. **`hdr` до 8–10** в conservative-creative — детали без выдумывания, лица не трогает.
3. **Несколько `reference_images`** у Seedream (до 5): подать оригинал + крупный кроп лица, чтобы
   модель держалась за черты. Штатная возможность API, не пробовали.
4. **Гибрид с маской**: генеративный результат для фона и одежды + оригинальные лица, склейка
   локально. Даёт и красоту, и сходство, но требует детекции лиц и аккуратного блендинга.
5. **`image-relight`** — отдельный эндпоинт под свет, без переписывания содержимого.

### Экономия кредитов (после расхода 1780 на исследование)

Тариф апскейлеров считается **по площади результата**, а не по факту вызова:

| Движок | Кредитов на Мп выхода |
|---|---|
| `seedream-edit`, `nano-banana` | фикс. 50 за вызов |
| `precision-v2` | ~18 |
| `creative` | ~26 |

Скрипт теперь печатает оценку до отправки (`estimate_credits`), чтобы дорогой прогон не уходил
случайно. Правила, вытекающие из тарифа:

1. **Подбирать настройки на уменьшенном входе.** `--downscale 1200` + `--scale 2` даёт 4.9 Мп
   (~130 кредитов) вместо 14.7 Мп (~400) — эффект ручек виден так же. Финал считать один раз.
2. **Не апскейлить «про запас».** Seedream сам отдаёт 3.7–4.2 Мп = 18×15 см при 300 DPI, для
   большинства слотов фотокниги этого достаточно; апскейл включать под конкретный макет.
3. **Бесплатные шаги делать всегда** — `local_fix` и `fit_aspect` не стоят ничего и снимают
   половину претензий (ориентация, цвет, геометрия).

### Себестоимость в рублях

Тариф владельца: **4100 ₽ за 45 000 кредитов → 1 кредит = 9.1 коп.**

| Маршрут | Кредитов | ₽/фото | Печать при 300 DPI |
|---|---|---|---|
| `local_fix` + `fit_aspect` | 0 | **0 ₽** | размер исходника |
| + `seedream-edit` (или nano-banana) | 50 | **4.6 ₽** | 18×15 см |
| + `precision-v2` до 14.7 Мп | 320 | **29 ₽** | 35×30 см |
| `creative` от оригинала (без генерации, лица целы) | ~200 | **18 ₽** | 25×21 см |
| + `creative` hdr до 14.7 Мп | ~450 | **41 ₽** | 35×30 см |
| **веб-версия (Seedream 5 pro)** | 1500 | **137 ₽** | 11×15 см |

Пакета 45 000 кредитов хватает на ~900 фото по базовому маршруту, ~140 по маршруту с апскейлом
до 35×30 см, и всего ~30 фото при работе через веб-интерфейс.

Всё исследование (13 вызовов, 1780 кредитов) стоило **162 ₽**.

**Следствие для продукта:** улучшать подряд все фото заказа дорого — при 30 фото это 138 ₽
на базовом маршруте и 870 ₽ с апскейлом. Отбор уже встроен: DPI-гейт `effectiveDpi < 200`
в book-editor показывает кнопку только там, где разрешения реально не хватает.

### hdr

`hdr 9` при `creativity −10` / `resemblance +10` / `magnific_sharpy`: микроконтраст, текстура ткани
и волос заметно резче, лица не затронуты — ручка безопасная. Проверено на уменьшенном входе,
14с, 2400×2048.

### Открытые вопросы

1. `scale_factor: 4` вернул x2 — причина не выяснена (гипотеза: нормализация входа). Нужен
   контрольный прогон с `--scale 8`.
2. v1 `image-upscaler-precision` отдаёт пустой `generated[]` — формат ответа другой либо только webhook.
3. Кроп и поворот в вебе — сделала модель или владелец до загрузки? От этого зависит, годится ли
   генеративный путь для пакетной обработки.
4. Цена кредита в деньгах и стоимость Seedream-прохода на объёме (100 фото ≈ 150 000 кредитов).
